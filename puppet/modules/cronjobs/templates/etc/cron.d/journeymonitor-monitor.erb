# managed by puppet
MAILTO=""

<% if @env == "prod" %>
* * * * * root /opt/simplecd/simplecd.sh tag travisci-build-master-* https://github.com/journeymonitor/monitor.git https://github.com/journeymonitor/monitor/commit/ | while IFS= read -r line;do echo "$(date) $line";done >> /var/tmp/simplecd/journeymonitor-monitor.log 2>&1
<% end %>

* * * * * root sleep 30 && sudo -u journeymonitor PHP_ENV=<%= @env %> /usr/bin/php /opt/journeymonitor/monitor/bin/import.php http://127.0.0.1:8082/api/internal/testcases.json >> /var/tmp/journeymonitor-monitor-import.log 2>&1 && rm -f /etc/cron.d/journeymonitor-run-* && sudo -u journeymonitor PHP_ENV=<%= @env %> /usr/bin/php /opt/journeymonitor/monitor/bin/create.php >> /var/tmp/journeymonitor-monitor-create.log 2>&1 && chown root:root /etc/cron.d/journeymonitor-run-* && chmod 0644 /etc/cron.d/journeymonitor-run-*

# Selenium runs create a lot of cruft in /tmp that needs to be cleaned up
*/10 * * * * root /usr/bin/find . -name "seleniumSslSupport*" -type d -mmin +60 | xargs rm -rf
*/10 * * * * root /usr/bin/find . -name "anonymous*webdriver-profile" -type d -mmin +60 | xargs rm -rf
