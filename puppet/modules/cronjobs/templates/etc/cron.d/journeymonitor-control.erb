# managed by puppet
MAILTO=""

<% if @env == "prod" %>
* * * * * root /opt/simplecd/simplecd.sh tag travisci-build-master-* https://github.com/journeymonitor/control.git https://github.com/journeymonitor/control/commit/ 2>&1 | while IFS= read -r line;do echo "$(date) $line";done >> /var/tmp/simplecd/journeymonitor-control.log
<% end %>
* * * * * root sudo -u www-data /bin/bash /opt/journeymonitor/control/import-testresults.sh http://127.0.0.1:8081/ <%= @env %> 2>&1 | while IFS= read -r line;do echo "$(date) $line";done >> /var/tmp/journeymonitor-control-import-testresults.log
