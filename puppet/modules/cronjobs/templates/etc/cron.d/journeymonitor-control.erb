# managed by puppet
MAILTO=""

<% if @env == "prod" %>
* * * * * root /opt/simplecd/simplecd.sh tag travisci-build-master-* https://github.com/journeymonitor/control.git https://github.com/journeymonitor/control/commit/ 2>&1 | while IFS= read -r line;do echo "$(date) $line";done >> /var/tmp/simplecd/journeymonitor-control.log
<% end %>
* * * * * root sudo -u www-data /usr/bin/php /opt/journeymonitor/control/bin/console journeymonitor:control:import:testresults http://127.0.0.1:8081/ --env=<%= @env %> 2>&1 | while IFS= read -r line;do echo "$(date) $line";done >> /var/tmp/journeymonitor-control-import-testresults.log
